# Base URL of the telemetry API.
# Leave empty to use the in-app MSW mock (no backend needed for the demo).
# Point it at the real API once the backend exists, e.g. http://localhost:3333
VITE_API_URL=

# Organization the client is scoped to (BR-ORG-02).
VITE_ORG_ID=org-demo
